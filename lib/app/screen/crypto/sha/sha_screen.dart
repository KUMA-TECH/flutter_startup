import 'dart:convert';

import 'package:flutter_startup/app/screen/crypto/crypto_screen.dart';

class SHAScreen extends CryptoScreen {
  SHAScreen({super.key}) : super(title: "SHA加密/解密");

  @override
  String encode(String? input) {
    String result = '';

    return result;
  }

  @override
  String decode(String? input) {
    String result = '';

    return result;
  }
}
